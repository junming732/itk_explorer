# Landmarks CMakeLists.txt

# Library sources
set(LANDMARKS_LIB_SOURCES
    LandmarkIO.cpp
)

# Create landmarks library
add_library(landmarks_lib ${LANDMARKS_LIB_SOURCES})
target_link_libraries(landmarks_lib ${ITK_LIBRARIES})
target_include_directories(landmarks_lib PUBLIC
    ${CMAKE_SOURCE_DIR}/include
)

# No executables in landmarks (just library)